import styles from "../../../common/styleConsts"

const { list: { display, FlexWrap, justifyContent } } = styles

export const listStyle = `
    .list {
        display: ${display};
        flex-wrap: ${FlexWrap};
    }
`